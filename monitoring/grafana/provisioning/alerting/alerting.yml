# Grafana アラート通知設定
# Slack通知は未設定のため無効化（GF_SLACK_WEBHOOK_URL が未設定）
# 設定する場合は contactPoints + policies を再追加し、以下の環境変数を設定すること:
#   docker-compose.yml の grafana サービスに:
#     - GF_SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXXXX

apiVersion: 1
