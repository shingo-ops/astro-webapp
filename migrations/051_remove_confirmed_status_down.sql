-- ============================================================================
-- ADR-021 J1 fix / Migration 051 ロールバック方針（実行不可・文書化のみ）
-- ============================================================================
--
-- 警告: 051 によって confirmed → pending に統合された行を migration 単体で
-- 元に戻すことはできない。元 confirmed 行を識別する追跡列が存在しないため、
-- "どの pending 行が元 confirmed だったか" を再現できない。
--
-- 復旧が必要な場合は、migration 051 適用 **前** に取得した DB バックアップから
-- 該当行の status をピンポイントで復元すること（運用判断）。例:
--
--   -- 適用前バックアップから tenant_004.orders を一時テーブルに展開し、
--   -- 元 confirmed 行の id を取得して本番に反映する手順（運用 runbook 別管理）
--
-- 本ファイルは「ロールバック手順が文書化済」であることを示すための
-- プレースホルダで、SQL としては何も実行しない。
--
-- 変更履歴:
--   2026-05-13: 初版（ADR-021 J1 fix）
-- ============================================================================

SELECT 1;  -- noop
